/** @type {import('./$types').PageLoad} */
export async function load() {
  return {
    pageMeta: {
      title: 'Frequently asked questions',
      description: 'A set of questions and answers about Apportionments.'
    }
  };
}
