/** @type {import('./$types').PageLoad} */
export async function load() {
  return {
    pageMeta: {
      title: 'Style guide',
      description: 'An internal page to test and view the styles for the site.'
    }
  };
}
